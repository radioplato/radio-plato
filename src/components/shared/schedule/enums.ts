export enum DAY_INDEX {
    Monday = 0,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday
};

export enum AZURA_SCHEDULE_TYPE {
    PLAYLIST = 'playlist',
    STREAMER = 'streamer'
};